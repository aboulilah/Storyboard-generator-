:root {
  --green: #16a34a;
  --white: #ffffff;
  --text: #111;
  --muted: #666;
  --border: #e5e5e5;
}

body {
  background: var(--white);
  color: var(--text);
}

.field-card,
.scene,
.o-card {
  background: var(--white);
  border: 1px solid var(--border);
}

.btn-main {
  background: var(--green);
  color: white;
}

.meta-pill {
  background: var(--green);
  color: white;
}

.scene-num {
  background: var(--green);
  color: white;
}
